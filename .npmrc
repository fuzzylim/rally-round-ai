node-linker=hoisted
# Ensure next resolution works properly
public-hoist-pattern[]=*next*
# Needed for proper dependency installation
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*react-dom*
shamefully-hoist=true

# Critical for Vercel deployment with pnpm workspaces
link-workspace-packages=true
shared-workspace-lockfile=false
prefer-workspace-packages=true

# Required for Vercel to resolve workspace dependencies
node-linker=hoisted
